% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/som.representation2.R
\name{som.representation2}
\alias{som.representation2}
\title{Perform Self-Organizing Map (SOM) Analysis and Plot Environmental Data}
\usage{
som.representation2(
  data,
  env.data,
  xdim,
  ydim,
  topo = "hexagonal",
  rlen = 100,
  alpha = c(0.05, 0.01),
  keep.data = TRUE
)
}
\arguments{
\item{data}{A numeric matrix or data frame where rows represent samples and columns represent variables.}

\item{env.data}{A numeric matrix or data frame of environmental variables with the same number of rows as \code{data}.}

\item{xdim}{An integer specifying the number of nodes in the x-dimension of the SOM grid.}

\item{ydim}{An integer specifying the number of nodes in the y-dimension of the SOM grid.}

\item{topo}{A character string specifying the topology of the SOM grid. Default is \code{"hexagonal"}.}

\item{rlen}{An integer specifying the number of iterations for training the SOM. Default is 100.}

\item{alpha}{A numeric vector of length 2 specifying the initial and final learning rates. Default is \code{c(0.05, 0.01)}.}

\item{keep.data}{A logical indicating whether to keep the original data in the SOM object. Default is \code{TRUE}.}
}
\value{
A list containing:
\itemize{
\item \code{som.model}: The trained SOM model.
\item \code{env.data1}: The aggregated environmental data by SOM units.
\item \code{groups}: A list of indices indicating which samples belong to each SOM unit.
\item \code{som.model1}: A SOM model where the codebook vectors are replaced with aggregated environmental data.
}
}
\description{
This function performs a Self-Organizing Map (SOM) analysis on the provided data and plots both the counts and the environmental variable codes.
}
\examples{
# Example usage:
set.seed(123)
data <- matrix(runif(150), nrow = 30, ncol = 5)
env.data <- matrix(runif(150), nrow = 30, ncol = 5)
som.representation2(data, env.data, xdim = 4, ydim = 4)


}
